import React from 'react';

const DashPrimaryHead = ({ children }) => {
  return (
    <div className='text-xl font-semibold text-black dashPrimaryHead'>
      {children}
    </div>
  );
};

export default DashPrimaryHead;
